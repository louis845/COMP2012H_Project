#ifndef DEBUG_H
#define DEBUG_H

/**
 * Change to 'true' or 'false'accordingly, whether debugging is needed. In debug mode unwanted behaviour (such as adding non-supported types together)
 * will be checked programmatically, with error thrown whenever such behaviour happens. 
 * All header files should include this header.
*/
#define DEBUG_MODE true

#endif